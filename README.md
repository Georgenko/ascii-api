Create ASCII art from an image/text using Fast API.  
There will be 3 main endpoints:  
One will accept a text prompt and will return a banner in ASCII art.  
The other will accept an image and convert it to ASCII art.  
The third will accept a text prompt, which will generate an image (most likely from a third party AI model), which will then be converted to ASCII art.
